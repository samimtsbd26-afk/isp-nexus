--- a/node_modules/node-routeros/dist/Channel.js
+++ b/node_modules/node-routeros/dist/Channel.js
@@ -125,6 +125,11 @@ class Channel extends events_1.EventEmitter {
                 if (!this.trapped)
                     this.emit('done', this.data);
                 this.close();
                 break;
+            case '!empty':
+                if (!this.trapped)
+                    this.emit('done', this.data);
+                this.close();
+                break;
             default:
                 this.emit('unknown', reply);
                 this.close();
